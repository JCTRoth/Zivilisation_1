-------------------
FUNCTIONAL TOODO'S:
-------------------
* Diplomatie: Exchange technologie with others, offer tech. for tech. offer tech. for money, AI should also do all of this. Tech. can only be exchanged when two units meet.
Gifting betters meaing of civ. about other civ. emensly. Recive of tech. only possible if tech. is unknown until now to civ. 
* Diplomatie: Add possiblity to send gold to other civs, and to request gold from other civs. The AI should be bribable, and the player should be able to bribe the AI. The AI should also be able to request gold from the player, and the player should be able to refuse or accept the request.
* One AI should be able to call other AI that is allied with for help when gets attacked of wants to attack others
* Sending money to the AI should improve the AI's relationship with the player, and requesting money from the AI should be possible in two ways: 1. The Player and the AI have a very good relationship, and the AI will give the player money for free. 2. Threten the AI with war, and the AI will give the player money to avoid war. Depending on the AI's personality, and Military strength, the AI may or may not give the player money. The AI should also be able to request money from the player, and the player should be able to refuse or accept the request. The AI should also be able to request money from the player, and the player should be able to refuse or accept the request. The AI should also be able to request money from the player, and the player should be able to refuse or accept the request.
  * AI to AI use this behavior as well, and the player should be able to observe this behavior in the Diplomacy screen if he has a embassy in boath civs.
* On the Auto End Turn screen, Inform about thr Auto End Turn feature, add there a check box to enable/disable the feature.
* Implement Human and Human (on one computer) against AI vs AI game modes.
* Implement End of game statistics that show the player how they did, enconmics, military, and science wise.
* Implement Civ1 ingame statistics screen that shows the player greatest cities in the worl, and there rank in the world wide demographics.
* High score system for single player games, including a list of high scores, and the ability to submit high scores to a server.
* Select the civilization on the start screen on initialization by the country the web browser is set to, or by the country the player is in (IP address).
    Therefor create a matrix mapping country codes to civilization ids, and use the country code to select the civilization on the start screen.


Far further development of the game engine is planned, including:

* Splitting up the game into client and server so that the game can be played over network.
* Networked multiplayer is not yet implemented, but the game engine is designed to support it in the future.
* Lobby system for multiplayer games, including a list of available games, and the ability to create and join games.
* Replay to play replays, I think it would make sense to use the save game, turn log, and map download do the replay stuff.
* Hall of fame for multiplayer games, including a list of high scores, and the ability to submit high scores to a server.

* Database backup and restore system, including the ability to backup and restore the database, and the ability to export and import the database.

-Open-

* TODO IMPORTANT FIND A GOOD WAY TO MANAGE THE SPECIALISTS!
* Add new ship type, "Trade Ship" 
A trade ship can establish a trade route from a city with a harbour to a city with another harbour,
and then the ship uses GoTo (pathfinding) to drive between the two cities automatically.
Everytime it was in one of city of your civilisation and than reaches than another city of your civilisation or of another civ.
the players get money.
One ship owner civ. gets 80% of the following calulation and the owner of the raget city 20%:
[(1+F*X)*(5*(P-10))+100+Cap)* (Minimum ([T*50%], [D*70%]))*20%/100]
F is 150% (1.5) if it is a Foreign Trade Route, 0 if domestic
X = 2*Enemy Civ. , 1*Your Own, 1.2 Friend, Peach Agreement NOT war.
P is the population of the city Getting the trade route
T is the population of the City the Trade Route is With
D is the Distance to the City
Cap is 25 if one of the cities is conected to the Capital, 0 if not
[] indicates rounding down
If this ship gets sunk by another ship than the owner civ. that where sinking the ship gets 100% of what the formular would had resulted,
Assume: The traget city in the formular is the ship.
D is distance to ship.
Pop is the population of the city the sunk ship came from.
T is the population of the city the sunk ship came from.

Still to do (bigger):
* Verify war commitment in a fresh AI-vs-AI run: with garrisons now able to
  mobilise, war declarations gated on a reachable target, and the unit cap
  taking the most conservative estimate, do wars produce sustained offensives
  and city captures? Watch attacks/round (counted from real COMBAT_* events now)
  and citiesCaptured in the next progression CSV. The AI's own "Attack —" lines
  are intent only and no longer counted as attacks.
* Naval invasion: land armies still cannot attack across water; the navy pivot
  builds ships but there is no transport/invasion mission (only colonies).
  Until this exists, an AI_VS_AI map whose strait splits the civs across two
  continents is an unwinnable stalemate (465-round run: 17 wars, 3 attacks).
* Multi-group offensives / siege commitment: two groups can form, but there is
  no coordination between them (no pincer/second-front planning).
* Army composition by era: the AI still over-produces the era's default
  defender; add stronger era/threat-aware composition weights.
* Economy buildings earlier: the AI reaches 0-8 gold with big armies; build
  marketplace/bank/temple earlier when upkeep pressure starts.
* Scout/mercenary grants from huts can exceed the corps cap (only production
  is capped); decide whether to convert/dismiss extras.







-----
BUGS:
-----


- 

?--Fixed--?
- The winning attacker no longer vanishes: only the loser is ever collected by
  the delayed corpse removal, and a city that is ASSAULTED in the same action in
  which its last garrison unit died no longer counter-strikes the unit that
  just won (that is what killed it "shortly after" the fight). All four winning
  paths (field kill, city capture, repelled city, losing attacker) are pinned by
  fake-timer tests in tests/defeatedUnitsStayDead.test.ts.
- City modal: the Raw JSON tab is gone (and its dead CSS with it).
- City modal queue: the first queued item can be moved up again — it is
  promoted to the current production (the old production drops one slot).
- City modal -> Citizens is now only an indicator: specialists are assigned from
  the side panel with icon/emoji toggles (hover explains the yields; the
  Entertainer is the default) and the worked-tile rows are read-only, because
  citizens are placed on the map (pick up a worked tile, drop on an idle tile).
- Manual citizen allocations are marked much more quietly (soft green wash +
  hairline edge) and the governor never writes its own choices into the manual
  set, so "manual" always means "the player put it there".
- Map: a sleeping unit no longer blocks picking up the citizen on its tile.
- Fortified and sleeping units are never called up by the turn manager: a
  fortified unit keeps 0 movement and stays "turns done" across turns (a garrison
  inside a city is not even drawn on the map, so there was nothing to act
  with), and a sleeping unit keeps its movement but is skipped by the unit
  queue. Unfortifying or waking hands the movement back, and the AI releases a
  fortified unit that is no longer a garrison so it can fight again.
- Side panel: the "1 citizen selected for reassignment" banner is gone. While a
  citizen is in hand a compact line above the "Selected City" heading says what
  is happening and how many idle tiles are available; placement stays a map
  action (the map highlights the idle tiles with a pulsing "+").
- Specialists are assigned on the MAP: the three icons drawn under the city
  sprite (Entertainer, Taxman, Scientist) are the selector — filled with a count
  when assigned, hollow when free. Clicking one converts a citizen that currently
  works a tile into that specialist; the click no longer opens the city screen.
  The drawing and the click hit-test share one exported layout function
  (getCitySpecialistButtons) so they can never drift apart.
- Side panel: "Specialists" is a read-only tally of how many the city has, by
  type (e.g. "🕺 Entertainer 2"), with no buttons — assigning is a map action.
- Side panel: while a citizen is being placed, one plain line inside the city
  block says where it came from, how many idle tiles there are and how to
  cancel. No farmer emoji, no close button, no yellow highlight.
- Side panel: the citizen block (specialist toggles + worked tiles + the city
  readout) now belongs to the selected CITY and is ALWAYS shown while that city
  is selected — neither the selected hex (picking a citizen up, clicking a
  radius tile) nor the turn manager's automatic unit focus can hide it. Only a
  selection the player made (a unit, or empty ground) hands the panel over.
- Research starts after the first 5 rounds instead of 3 (RESEARCH_UNLOCK_ROUND).
- New one-time "Tired of clicking End turn?" offer on move 15: a hero badge, one
  sentence of what Auto. turn ending does, and a miniature of the side-panel
  header (gold bar + the checkbox, glowing) that shows WHERE it lives instead of
  a paragraph about it. Two buttons, a fine print, never asked twice.
- Fix: waking a unit did not clear areTurnsDone, so a woken unit was never put
  back in the turn queue; moving or attacking did not clear isSleeping either,
  so a manually moved unit was skipped again the next turn.
- tests/unitStates.test.ts pins the meaning of every unit state (fortified,
  sleeping, skipped, waiting, embarked, defeated) against the turn reset, the
  unit queue and the can-I-act API, so a regression names the state that broke.
- CI: a new "Unit Tests" workflow (type-check, lint, vitest) gates every push and
  PR. The Playwright workflow only ran the browser suite, so all engine tests
  were ungated. knip runs there too, advisory for now: the repo has ~54 unused
  exports to clean up (tracked below) before it can fail the build.
- knip.json now includes tests/ and e2e/ as entry+project files (test-only
  exports used to look unused) and ignores the four legacy engine classes
  (src/game/{City,Civilization,Unit,Terrain}.ts) that nothing imports.
- ESC now deselects the city as well (with no dialog open): it clears the
  transient selection AND the persistent "focused city" marker, so the city net
  and its worked tiles disappear from the map too. A pending action still wins —
  a carried citizen is put back, a group selection is dropped — and the map's
  hex highlight is cleared along with the city.
- Coastal auto-production builds a Harbor in a safe coastal city (right after
  defense; defenders still come first and a city under direct threat never
  diverts to the Harbor). Adopted on small islands already.
- The selected-city map preview now shows the Food/Production/Trade yield ICONS
  with the amount underneath instead of the old dark number dots.
- A selected unit survives the turn change completely: emptying the unit queue
  at the END phase no longer wipes a human unit the player is watching, so
  TURN_END / AI_CLEAR_HIGHLIGHTS re-assert it and the player resumes with the
  same unit selected (a group selection does clear at the boundary).
- Research only starts after the first 3 rounds: no "No Research Selected"
  prompt and no auto-pick before the unlock round; the top-bar indicator says
  "Research starts soon" during the opening rounds instead of nagging.
- Multi-unit selection: shift/ctrl/cmd-click gathers units from several tiles
  into the group, a group can be ordered onto friendly-occupied tiles, the
  stack modal has All-movable/Clear buttons, and right-click/ESC cancels.
- City screen has a new "Units" tab: garrison + units supported by the city
  (status, HP, attack/defense, veteran badges) with a Select button.
- Selecting a unit no longer stops the auto turn manager: a hand-made selection
  lock is released as soon as the selected unit cannot act anymore (no moves /
  sleeping / fortified / skipped / dead), so the automatic next-unit focus and
  the end-turn flow resume.
- A selected unit now STAYS selected when the auto turn manager advances the
  turn (TURN_END / AI_CLEAR_HIGHLIGHTS / nextTurn keep the human's own unit and
  re-assert it as a hand-made selection; a dead unit is dropped on turn start).
- Save/Load restores fog of war: the human visibility/explored arrays are saved
  with the map and rebuilt on the store map on load; the fog arrays now use the
  real map width as stride (the 80-column default corrupted fog on smaller
  maps); turn manager round/player/phase, the turn queue and GoTo paths are
  restored too (paths through the GoToManager so both stores agree).
- Unit stack modal: clicking a city or a field with multiple own units opens a
  list (icon, name, veteran/status badges, health bar, attack/defense) where the
  player activates one unit or ticks 0..N units and then clicks a map
  destination to move every selected unit that still has movement points (gold
  rings mark the group selection; cities get a "City details" button).
- The store's activePlayer/currentTurn are now synced from the engine at turn
  start (the store previously kept its initial value, so turn-dependent UI ran
  for the wrong player).

- the GoTo and the MovePreview should be joint, code and visually
- attacking doesnt work if it goes through a friendly unit
- when moving units over enemy cities, they attack them instead
- when attacking, don't set that as a path that will be done automatically next turn
  -> just because unit can't reach that path (because it is blocked by enemy unit) it shouldn't auto attack next turn

- when citiy is selected, tiles could preview which resources they support
- improve building stuff ui in cities
- Only show the Tosts that are related to the player 
- bribe unit is bugged?
- nothing happens when producing caravans, fix it!
- Make it impossible to research somthing that is already discovered/researched by the civ.
- per turn projections are wrong
- when creating a city next to enemy unit, the city will imediately fall in enemies hand
- when attacking city with one health the city stays alive and only next turn disappears
- are the libraries of other cities even doing anything? i am getting one science while having multiple cities with libraries
- buying units directly should not be 50% cheaper
- why is research prograss towards the end of a research getting slower?
  ->  it should be constant right?
- resources in cities don't seem to be calculated correctly
  what is starvation doing?
  Here is how the starvation logic works written out as structured pseudo-code, followed by an exact breakdown of how the Granary functions in Civilization 1.Civ 1 City Food & Starvation Pseudo-codetext// Run once per city at the end of every turn
FUNCTION UpdateCityFood(city):
    // 1. Calculate Food Production
    total_food_produced = CalculateTilesFood(city.worked_tiles)

    // 2. Calculate Food Consumption
    citizen_consumption = city.population * 2
    
    settler_consumption = 0
    IF city.government_type IN [DESPOTISM, ANARCHY, MONARCHY, COMMUNISM]:
        settler_consumption = city.supported_settlers * 1
    ELSE IF city.government_type IN [REPUBLIC, DEMOCRACY]:
        settler_consumption = city.supported_settlers * 2
        
    total_consumption = citizen_consumption + settler_consumption

    // 3. Determine Net Food Status
    net_food = total_food_produced - total_consumption

    // 4. Apply Net Food to Storage
    city.food_storage = city.food_storage + net_food

    // 5. Check for Starvation
    IF city.food_storage < 0:
        city.population = city.population - 1
        city.food_storage = 0
        TriggerNotification(city.name + " starves! Population reduced.")
        
        // Handle city elimination if population hits zero
        IF city.population <= 0:
            DestroyCity(city)
            
    // 6. Check for Growth (For Context)
    ELSE IF city.food_storage >= city.growth_threshold:
        city.population = city.population + 1
        IF city.has_granary == TRUE:
            city.food_storage = city.growth_threshold / 2 // Granary retains half
        ELSE:
            city.food_storage = 0 // Standard reset
        TriggerNotification(city.name + " grows to size " + city.population)
Granary WorksIn Civilization 1, the Granary (costing 60 shields to build) has two distinct mechanics.
It serves as an accelerator for population growth and a shield against an unpredictable random event.
0. It stores food in it self when a city has a food surplus.
The size of it is 20. When the city has no food any more it uses the food storage until it is empty.
1. The Growth Retention Benefit (Primary Function)When a city grows,
it normally empties its entire food storage box back to 0.
Without a Granary: If your food box needs 20 food to grow,
it resets to 0 / 20 (or 0 / 30 for the next size) the moment the city population increases.
With a Granary: The game retains 50% of the food box threshold upon growth.
If the city needed 20 food to grow, the box instantly resets to 10 instead of 0.
This effectively cuts the time it takes for your city to grow a subsequent time in half.
2. The Disaster Immunity Benefit (Secondary Function)Civ 1 features random "Disaster" mechanics that
can strike individual cities during the turn flip. One of these disasters is Famine.
Without a Granary: If a random Famine event hits your city,
it immediately kills 1 citizen and wipes out a portion of your stored food,
even if your tiles are producing a food surplus.With a Granary:
The city is 100% immune to the random Famine disaster.
Important Note on Starvation: As shown in the pseudo-code above,
the Granary does not prevent turn-by-turn starvation caused by mismanagement.
If your tiles produce less food than your citizens and settlers eat (net_food < 0),
the food storage will eventually drop below zero, and a citizen will die regardless
of whether a Granary is present.If you are trying to calculate the exact food storage
capacity threshold for a specific city size to plug into your formulas.


------------
Water stuff
------------
?-Working-?

- it should not be possible to build water units on cities that have no water connection
  -> I can not move water units on water / river if next to it (probably not supposed to be)
* Introduce new Tile Type Lake if less than 4x6 Tiles than it is NO/NOT ocean but a lake, a lake has the same properties as an river but is NOT passable.
Integrate it into the map generation, and into the game engine, and into the game logic, and into the game UI, and into the game AI, and into the game diplomacy, and into the game economics, and into the game military, and into the game science, and into the game culture, and into the game religion, and into the game espionage, and into the game trade, and into the game diplomacy, and into the game victory conditions.
* The AI should detect not accessable areas if the AI descides that it needs to move there it should build ships if possible,
if not exclude this as target until ship building is possible, to found a city on a connected water should also be planned if not city on a connected water.
* Units should in general use the GoTo pathfinding to reach there targets, and check on the way the the destination if new target gets set by them.
* The AI should start to build ships if no enemy is reachable on the land way.
Rivers are walkable and passable for the Player, AI and pathfinding.
Exmaple 1 (L: land, R: river, number: index):
L R L
L R L
L R L
Unit can pass the river from left to right and right to left
Example 2:
1 L R R L
2 L R L L
3 L R R L
Unit can pass from left to right and right to left only here on index 2
Unit can not walk down the river.
Example 3:
1 L R R L
2 L R L L
3 L R L L
4 L R R L
Unit can walk down up,down on 3,4 but and not move from index 3 R down to index 4 R
Implement it in all of the game, centrlize and use constants where good and possible
Rivers can contain fish also only 1/2 of % that tile as fish feature than Ocan tile.

Improve the city managemnt of the AI.
It should get and advanced management of the city.
The AI should manage Food, Production, Money/Gold fitting the siution and strategy given by the AICoordinator
The AI should by all means prevent femens therefor it uses city management, if needed granary (long term plan to have 
build a lot of settlers/expansation) or a low surplus of food production over all.
The AI should use irrigation of settlers around the city.
Building irrigations requires settler that need food them self so this should be part of an equation but should be done if fitting in strategy and benefitting.
City growth is important, needs stable surplus of food.
Also: Ai: the more far away a village from next town and the more towns the civ. hat the higher % that AI takes village instead of ignoring it to not get over runned by babrians.  

- TODo -
* improve the budget keeping / money handling of the AI, not overweight but should better do it use all mesures for it
* Remove "Forge" from the game and AI strategy also higher prio. to harbor when detects that on smaal island alone,
harbor should in general get a higher weight in the Auto Build when needed,
never build if not needed, on very smaal islands even more
* If Island is very small than highest prio to building ship
* If AI detects by ship a smaal island with no city on it, send a settler and build a city there
* Improve the AI scout and in general unit movement on islands

* The AI should load units into transport ships
Here is the technical breakdown of the Civ 1 transport backend systems, adapted strictly for your game's specific rules (removing stack death and adding the unit selection modal):
## 1. The Active Container & Array System
In the Civ 1 engine, units do not possess an "embarked" flag status like in modern games. Instead, the game relies on a strict coordinate alignment :
* **Tile Sharing Logic:** A land unit is considered "on board" a transport ship if its grid coordinates (X, Y) match the transport's grid coordinates exactly over a water tile.
* **Capacity Cap Enforcement:** The engine actively checks the transport's data array capacity whenever a land unit moves onto its tile. If a ship already has its maximum number of land units sharing its X,Y coordinate, the engine treats that tile as an impassable barrier, completely blocking movement.
* **The sleeping Automatic Boarding:** When a transport ship exits a city tile, the code executes a query check for any land units flagged as sleeping (S key) or Sleep inside that city. If found, the engine changes their coordinates to match the moving ship, dragging them along as cargo automatically.

### 🆕 Unit Selection Modal (Cargo Management UI)
Because multiple units can share the exact same transport tile, a **Unit Selection Modal** is required for player interaction. 
* **Trigger:** When a player selects a transport ship and issues an "Unload" or "Activate" command.
* **Functionality:** A modal popup appears displaying the icons and stats of the land units currently assigned to that ship's coordinates. The player selects the specific unit they wish to wake up or unload, preventing accidental activation of the wrong unit in a crowded tile.

## 2. Deep Ocean Sinking Formula (Trireme Math)
The early-game Trireme uses a specific probability check if it finishes its turn on a deep ocean tile (dark blue) rather than a coastal tile (light blue):
* **The Cargo Wipe:** If the RNG determines the ship sinks, the engine executes a deletion loop for that tile's coordinates. It deletes the ship unit first, then systematically deletes every single land unit ID sharing those exact coordinates. **If the ship sinks, all units inside the ship are destroyed instantly.**
* **The Lighthouse Counter:** Building The Lighthouse World Wonder modifies this logic. The wonder acts as a flag modifier that shifts the Trireme’s sinking probability function to 0%, making early-game deep-ocean transport perfectly safe.

## 3. Combat Resolution (Individual Defense)
*Note: The classic Civ 1 "Death Stack" mechanic does not exist in this game.*
* **Individual Combat:** When an enemy naval unit attacks a tile containing your transport ship and any escort vessels, combat is resolved strictly on a 1-on-1 basis. The attacker engages the specific unit occupying the defensive slot; escort ships do not automatically absorb hits for the transport.
* **Destruction Consequence:** If the transport ship is directly attacked and loses the combat resolution calculation, it is destroyed. Because of the Cargo Wipe logic, all land units stored in its coordinate array are instantly destroyed alongside it. 

## 4. The Infinite Movement Exploit: "Ship Chaining"
Because of how the Civ 1 engine resets and checks unit movement points (MP), players can take advantage of a logic loophole:
* **The Movement Variable Reset:** When a transport ship moves, it consumes its own MP. However, the land units sitting inside the transport do not consume their land-based MP.
* **The Relay Hand-off:** If you park two transport ships on the exact same tile, you can manually "wake up" (W key) the land units inside Transport A. The engine sees them as standing on a valid transport tile, so they still have full movement points. You can then command them to "Sleep" again, and the engine will register them as occupying Transport B.
* **The Infinite Loop:** By lining up ships across the entire ocean map—each spaced exactly at the maximum movement range of the previous ship—you can hand off units from ship to ship. The engine allows you to move an army across a massive ocean map in a single turn, bypassing the turn timer mechanics entirely.

## 5. Implement a new modal when clicked on a tile where multipile units are at the same time it opens, shows all units with there status (fortified, sleeping and so on, with health bar)
by clicking on a defening or sleeping unit I activate the unit.


## 6. Cities have no limit on units inside them.
Extend the city menu by new tab that works the same way the modal for unit managment works
Dont show the units fortified in the city directly on the map but show the fortified icon of it on the city


DONE — Harbor ocean-food replaced by the Fisher Boat:
* Removed the Civ1 Harbor "+1 food from every worked OCEAN tile" bonus
  (EconomicManager.tileYieldsForCity is gone).
* Fisher Boat (fisher_boat): 20 shields, requires the Harbor BUILDING, hold 6
  fish, movement 2, attack 0 / defense 1, max ONE per city (ProductionManager
  rejects a second; the city modal shows its status for harbor cities).
* "Deploy Fishing Net" in the unit context menu (fish tiles only, ocean or
  river). "Remove Fishing Net" is a recall: home to unload, then automatically
  back to the same net tile (route kept).
* Auto-route: outbound → fishing (1 fish/turn until 6) → inbound → unload →
  outbound, driven by GameEngine.advanceFishing each turn.
* Distance formula (unchanged): foodPerFish = min(3, 1 + floor(d/4)) with d =
  Chebyshev distance home city ↔ net tile → 6 / 12 / 18 food per full catch.
* Unload fills city.foodStored up to foodNeeded; a catch that does not fit
  grants a one-round +2 food bonus at the next growth step.
* NEW: a Fish tile only pays its full food to a city while a Fisher Boat is
  actively using it — without a boat the city draws 1 food less per fish tile.
* AI: AutoProduction builds one boat per harbor city under food pressure, and
  AIManager sails it to the nearest known fish tile and deploys the net.

- AI vs AI analysis (session aivsai-2026-09-20T23-11-44-788Z) -
Fixed directly:
* civ.score was 0 for the whole game in the progression CSV. VictoryManager
  now computes the Civ1 score every turn end (population, land area, cities,
  techs, wonders).
* Harbor: inland cities can no longer build it (ProductionManager rejects
  'no_water_access'); the AI gives it 0 priority inland and a coastal bonus.
* Failed-move memory is now used by ALL land units (not only scouts), so
  repeated move_failed loops stop sooner.
* Army groups: reachable rally points, rally timeout, vanguard attack,
  committed groups ignore retreat, land-only targets, reserve keeps 3 units
  free, persistent bulk plans.

Also fixed after the analysis:
* War commitment: aggression at war is weighted higher (+12) and a recent
  city loss triggers retaliation (+15, and the capturer is preferred as a
  bulk-attack target for 15 rounds).
* Army upkeep / gold starvation: the sustainable-unit cap is now enforced for
  the CURRENT production too (not just queued follow-ups), and the default
  army mix caps defenders at ~2 per city unless the city is threatened.
* Move failures/skips: the failed-tile memory is shared by all land units and
  a committed target on a blocked tile is dropped (stuck recovery).
* Scout overproduction: scouts queued across ALL cities are counted and the
  corps is hard-capped at 3.
* Score completeness: pollution penalty and peace-years bonus implemented.
* Eliminated civs are emitted once in the compact CSV and then dropped.

- Implement City Wall functionality also show the city wall on the city visually
- CLEANUP: ~54 unused exports reported by knip (mostly exported-but-internal
  tuning constants in src/game/engine/AI/*). Either un-export them or reference
  them from a test, then make the knip CI step a hard failure.
